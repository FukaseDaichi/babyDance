# Project Learnings

<!--
記法ルール:
- 1項目1洞察。複数の学びを1行に詰めない
- 各項目の先頭に日付を必ず入れる（例: - 2026-08-24: ...）
- 上4セクションは「生の観察」の置き場。Consolidated Principles には
  統合パスで抽出した原則だけを置く。両者を混ぜない
-->

## Patterns That Work
（効いたやり方・型）
- 2026-09-04: 生成物の GUID 安定性は、生成を 2 回走らせて `.meta` の guid を突き合わせて証明する。1 回の観測では「変わらない」ことを示せず、安定していると思い込む余地が残る。
- 2026-09-04: テストのフィクスチャを対称な値（同じクリップ長・同じ係数・既定オフセット）で組むと、単位の取り違えも対象の取り違えも誤差が相殺して不可視になる。1 つの軸を非対称にするだけで複数の穴が同時に閉じる。
- 2026-09-04: ビルド成果物を人手検証に渡す前に、起動してログの例外を grep するだけの短いスモークテストを挟む。起動時に走る初期化が契約の検査を兼ねていることが多く（本件では Animator の State 名解決）、人間の時間を使う前に潰せる。
- 2026-09-04: Editor ツールの「人手値は保持・生成値は上書き」というラウンドトリップを GUI なしで検証する型: 対象 `.asset`（素の YAML）を `sed` で手調整値に書き換え、生成メソッドを実行し、ログ出力とファイル再読込の両方で確認する。
- 2026-09-04: Unity リポジトリに `.agents/` や `.claude/` などツール系ディレクトリを新設したら `git check-ignore -v` で除外を確認する。Unity 標準の .gitignore は除外パターンが広範で、追跡されないまま「ローカルでは動く」状態を作りやすい。
- 2026-09-05: 「再生成のたびに差分ノイズが出る」型の負債は、直す前に一度生成を走らせて差分行数を測ってから直す。修正後のゼロ差分は、直前に非ゼロを見ていて初めて証拠になる。
- 2026-09-05: 生成ツールの削除経路は、使い捨ての入力（既存 FBX のコピー）を足して生成し、入力を消してもう一度生成する形で end-to-end 検証できる。新規に作られたアセットしか消えないので、シーンが GUID で参照する既存アセットを壊さずに済む。
- 2026-09-05: Chrome で Unity WebGL のファイル選択を検証する型: ボタン押下で開いた OS のダイアログは Escape で閉じ、display:none の input は a11y ツリーに出ないので JS で表示に戻してから find → file_upload で流し込む。アップロード上限 10 MB に合わせて音源は短く生成する（60 秒・22.05 kHz で 2.6 MB）。
- 2026-09-05: 「どちらの条件で落ちたか不明」なエラーは、判別に要る値（null か、length・loadState はいくつか）をエラーメッセージ自体に載せて 1 ビルド使う。調査用の計測がそのまま製品のエラーメッセージ改善として残る。
- 2026-09-05: 「値が純関数である」設計は、カメラのような時間依存の演出にも横展開できる。姿勢を拍の純関数にしたので、カット境界・着地の静止・サイクル一致を EditMode で決定的に検証でき、時間源を増やさずに済んだ。
- 2026-09-05: clamp のようなガードは、譜面の中に「ガードが実際に効く箇所」（ピッチ −20 に Shake を重ねる等）をわざと作っておく。全走査テストはそれで初めて「ガードを外すと落ちる」ようになる。ガードを外して落ちることまで確認して初めて完了。
- 2026-09-05: アプリ内 Browser（file_upload の無い方）で Unity WebGL のファイル選択を通す型: 「開く」を押して OS ダイアログを Escape で閉じた後、served ディレクトリに置いた WAV を fetch → DataTransfer で input.files に入れ input.onchange() を直接呼ぶ。10 MB 制限は無いが、WAV は Builds/ 配下（gitignore 済）に置く。

- 2026-09-05: 外部サービス（Mixamo 等）が弾く 3D アセットは、まず FBX をバイナリ構文で直接パースして Objects の種類と件数・埋め込み Video の Content サイズを数える。8.6 MB のうち 8.1 MB が埋め込みテクスチャ、といった「重さの出どころ」が推測なしで確定する。
- 2026-09-05: モデルの姿勢（T ポーズか、四肢が胴から離れているか）は Blender の `--background` で WORKBENCH の正投影 3 面レンダを吐いて画像で確認する。頂点数や bbox だけでは自動リグの前提を満たすか判定できない。
- 2026-09-05: リターゲットの破綻は絶対値では判定できない。新キャラと既存の基準キャラ（XBot）に同じクリップを SampleAnimation で 20 点サンプルし、全 HumanBodyBones の最小 Y を並べて比較する。単独で見ると異常に見える minBoneY=-0.09 も、基準キャラが -0.089 なら正常と確定できる。
- 2026-09-05: Blender 往復（マテリアルスロット追加のための再エクスポート等）でリグを壊していないことは、往復前後で同じクリップの minBoneY / maxBoneY を測って一致を見れば証明できる。本件は -0.0392 / -0.0442 が完全一致し、ボーン数 33・未割当頂点 0 と合わせて往復の無害性が確定した。
- 2026-09-05: カメラ譜面は距離ではなく「画面に収める高さ [m]」で書き、距離は d = h / (2 tan(fov/2)) で逆算する。キャラの体格が変わっても書き直すのは 1 か所の語彙定数だけになり、テストも「そのフレーム高はこのキャラに対して妥当か」という意味のある不変条件で書ける。
- 2026-09-05: 純関数のカメラテストに加えて、実キャラのボーンを踏ませた射影チェックを 1 本持つと強い。ダンスをサンプルしながら ShotDirector.Tick を回し、Head/Hips/Foot/Hand を WorldToViewportPoint して画面外フレーム数を数える（本件は引き画 308 フレームで 0 件）。EditMode では Awake が走らないので、ShotDirector の Camera 参照はリフレクションで埋める。
- 2026-09-05: 実行時にしか分からない値（注視点の高さ）に依存するガードは、その値を引数に取る純関数として CameraScore 側に置く。MonoBehaviour 側に書くと EditMode から呼べず、配線の変異（呼び出しを消す）を検出できない。Offset(pose, targetHeight) にしたことで「全拍 × 想定される注視点高さ 9 通りで床下に出ない」を総当たりでき、呼び出しを外す変異で実際に落ちることまで確認できた。
- 2026-09-05: WebGL ビルドの起動スモークテストでは「console に例外なし」と「シーンが立ち上がった」を別の証拠として集める。Unity スプラッシュが数秒あるので、console が clean な時点のスクリーンショットはまだ Unity ロゴのまま。splash 通過後に撮り直して UI とキャラの描画まで見て初めて起動確認になる。
- 2026-09-05: 「このキャラに表情を付けられるか」は shape_keys の有無だけでは決まらない。テクスチャを縮小して 1 枚見るだけで方式の半分が消える。Meshy の UV は三角形単位に粉砕されており顔が連続した島を成さないため、UV オフセットによるテクスチャ差し替えも手描き修正も最初から成立しない。
- 2026-09-05: 顔の造作の 3D 座標は、既知のフレーミング（中心・幅・解像度）で正射影レンダを吐き、Pillow で画素クラスタの bbox を取れば数値で確定できる。仕様に目分量を書かずに済む。検出結果を元画像に矩形で重ねて描き戻すと、閾値自体が正しいことも同時に検証できる。
- 2026-09-05: 曲面に沿う「デカール殻」は、格子平面 → shrinkwrap(PROJECT) → 法線方向オフセット で作れる。格子の素の 0..1 UV がそのまま使えるので、元メッシュの UV レイアウトがどれだけ壊れていても影響を受けない。頂点グループを 1 本のボーンにウェイト 1 で与えれば追従も無料で付く。
- 2026-09-05: 「この spec は別のエージェントが実行する」と決まった時点で、spec に要る精度が変わる。人間向けなら「目のあたりを覆う」で通るが、実行させるなら座標を全部確定させ（目分量を 1 つも残さない）、合否の判定条件を実行できる形で書き、自動では守れない項目を明記する必要がある。読者が誰かを先に確認してから書き始める。
- 2026-09-05: Unity リポジトリのコミット前は、アセット本体と `.meta` の追跡状態が対になっているかを `git ls-files` で確認する。本体だけ追跡され `.meta` が未追跡だと（本件では Meshy 原本テクスチャ 6 枚と .glb）、別環境で GUID が再生成され、マテリアルが GUID 参照するテクスチャが無症状で切れる。`.meta` の GUID をマテリアル側の guid と grep で突き合わせれば、必要な `.meta` が機械的に決まる。
- 2026-09-05: 方式の却下は文章でなくプロトタイプで行う。「本体メッシュから顔ポリを切り出す」案は、実際に作って初めて前髪と顔ポリが交互に並ぶこと・平面投影 UV に反転ポリが 253 中 4 個出ることが分かった。机上では「顔を分離すればよい」としか書けない。
- 2026-09-06: 既存の骨座標を完全一致で保つFBX加工では、Blenderの全体再出力による約10⁻⁷ mの丸めも実測して区別する。Blender付属のFBXコーデックで生成したデカール要素だけを元FBXへ追加すれば、既存の本体・骨の数値を保ち、4ダンス×55種のHumanBodyBonesの記録を完全一致にできる。
- 2026-09-06: リグ付きメッシュへのshrinkwrapはレスト姿勢で行う。変形後の本体へ投影した殻に同じArmatureを付けると頭の変形が二重にかかり、殻が顔の裏に入る。正面の瞳残存15,527pxがこの失敗を直接検出した。
- 2026-09-06: 顔デカールのベイクは、表面法線任せだと局所的な凹凸でレイが外れる。正面方向の明示的なケージで投影し直すと、目9px・口5pxの未取得画素を透明度の上書きで隠さず解消できる。
- 2026-09-06: 目と口のデカールが重なると、不透明マゼンタの被覆検証が通っても、口のアトラスに元の瞳が写り込む。レイヤーごとのコマ0も画像で確認し、各造作が互いのパッチに入らない範囲へ分ける必要がある。

- 2026-09-06: ベイク由来の開口を閉口へ加工するときは、保存PNGの暗画素面積で閉口・半開き・開口の差を検証する。閉口を未加工ベイクに戻す変異で失敗を確認でき、ファイル生成成功だけでは見逃す意味上の誤りを検出できる。

## Mistakes to Avoid
（失敗と再発防止策）
- 2026-09-04: プロジェクトの実行ルール（本リポジトリなら「Python は必ず `uv` 経由」）は、成果物のコードだけでなくファイル編集用のワンライナーにも適用される。補助作業だからと素の `python3` を使って自分で破った。
- 2026-09-04: 変異テストを設計するとき、その変異が数学的に無効でないか先に確かめる。近接値の差を足し戻す形（`x += cur - x`）は IEEE-754 で `x = cur` と bit 一致するため、累積バグを検出する変異として機能しない。
- 2026-09-04: 多層ガードでは外側の網が先に発火するので、内側の網が出すはずのメッセージを検証手順の合格条件に書いてはいけない。正しい実装でも期待した文字列は現れない。
- 2026-09-04: 「確認できた」という検証報告は、測った量が本当に判別的かを別に確かめる。振幅が同じで周波数だけ違う信号を振幅で測れば、正しくても差は出ない。
- 2026-09-04: 生成ツールがアセットを delete→create で作り直すと GUID が変わり、シーンに焼かれた参照が無症状で切れる（例外も出ず、UI も正常に構築され、対象だけ動かない）。既存アセットを読み込んで中身だけ差し替える。
- 2026-09-04: Unity の `ModelImporterClipAnimation` は C# プロパティ名と `.meta` の直列化キー名が一致しない（`lockRootRotation` → `loopBlendOrientation`、`lockRootHeightY` → `loopBlendPositionY`、`lockRootPositionXZ` → `loopBlendPositionXZ`）。プロパティ名で `.meta` を grep する検証手順は正しい実装でも空振りする。
- 2026-09-04: AGENTS.md の「LEARNINGS.md ループ」指示はサブエージェントにも発火し、実装者が各自 LEARNINGS.md へ追記しようとする。dispatch 側で明示的に禁止し、観察は報告経由で集約する。
- 2026-09-04: awk のセクション範囲カウントをファイル末尾への追記で検証すると、追記行が最終セクションに落ちて 0 が返り、カウンタの故障と誤読する。範囲判定のテストは対象セクションへの挿入位置を明示して行う。
- 2026-09-05: gh-pages ブランチを初回 push すると GitHub 側が Pages を自動有効化し、直後の有効化 POST が 409 で失敗する。有効化の成否は POST の戻りではなく、その後の GET で判定する。
- 2026-09-05: MMD 系資料の FOV / 距離の目安値をそのまま Unity に入れると、1.8 m のキャラでは半身が切れ、肩越しで頭部が画面を埋める。h = 2·d·tan(FOV/2) で「見せたい高さ」から距離を逆算してから譜面に入れる。
- 2026-09-05: WebGL で Loaded 直後に Play を押すと、ボタンの interactable 更新前でクリックが捨てられる。バッチ操作では Loaded メッセージを確認してから Play を押す。

- 2026-09-05: キャラの身長が変わったとき、カメラ距離を身長比で一律スケールするのは誤り。チビ体型は頭が全高の 31%（人型は 13%）なので、全身ショットと顔ショットで必要な倍率が違う。ショットごとに「何を画面に収めたいか」で決める。
- 2026-09-05: docs への蒸留は、表の行がファイル先頭に紛れ込む形で壊れることがある（カメラ 3 型の行が `# babyDance 設計` より上に置かれていた）。蒸留後は見出しと表の構造を `grep -n '^#'` で確認する。
- 2026-09-05: WebGL の release ビルドの build.log には Unity.Collections の AtomicSafetyHandle について "Failed to resolve" が必ず出る（AtomicSafetyHandle は development ビルドにしか存在しない）。AGENTS.md の `grep -E "error CS|Failed"` をそのまま合否判定に使うと、成功したビルドを失敗と読む。判定は BuildReport の result / totalErrors と done マーカーで行う（tools/unity.sh はそうなっている）。
- 2026-09-05: Blender の外部アドオンが動くかを `addon_utils.enable` 後の `preferences.addons` 照合で判定して「失敗」と誤読した。実際の証拠はログ側の `BlenderMCP addon registered` にあった。プローブを書いたら、それが測っている量が本当に判別的かを先に確かめる。
- 2026-09-05: scratchpad に stdlib と同名のスクリプト（`inspect.py`）を置くと、同ディレクトリで `uv run` した別スクリプトの `import numpy` が壊れる。エラーは numpy の C 拡張読み込み失敗として出るので原因が見えない。作業用スクリプトに stdlib と同名を付けない。
- 2026-09-05: AGENTS.md の「Python は必ず `uv`」は調査用のワンライナーにも適用される。PyPI のメタデータを見るだけの用途で素の `python3` を使って自分で破った。2026-09-04 に同じ失敗をしており、再発である。
## Domain Knowledge
（業務・仕様に関する事実）
- 2026-09-05: 同一の作業ツリーで別の Claude セッションが並行して編集していることがある。git status に自分が触っていない変更が現れたら、ps と lsof -d cwd で他プロセスの作業ディレクトリを確かめる。相手が TDD の red 段階だと、こちらのコンパイル・ビルド検証が相手のテストで落ちる。
- 2026-09-04: superpowers の finishing-a-development-branch は feature ブランチからの統合を前提に 3 択（base へマージ / PR / 現状維持）を出す。main で直接作業した場合はどれもそのままでは当てはまらないので、実状（push する / ブランチに移して PR / ローカルに留める）に組み直して提示する。
- 2026-09-04: Unity のテストランナーはテスト失敗時にプロセス終了コードを 2 で返す。終了コード判定を結果 XML の件数判定より先に置くと、件数ベースの詳細メッセージは通常の失敗経路では到達しない。
- 2026-09-04: Unity 6 の `GameObject.CreatePrimitive` はアクティブなレンダーパイプラインの既定材質を割り当てるため、URP プロジェクトでもマゼンタにならない。旧バージョンの「ビルトイン材質が付いてマゼンタになる」という経験則は通用しない。
- 2026-09-04: Mixamo の「Without Skin」で落としたダンス FBX も、`animationType = Human` + `avatarSetup = CreateFromThisModel` で有効な Humanoid Avatar を生成する。スキン無しでも Humanoid リターゲットの供給側になれる。
- 2026-09-04: Mecanim の `AnimatorStateInfo.normalizedTime` はラップせず増加し続けるため float 精度が総再生時間とともに劣化する。無効化した Animator を手動 `Animator.Update` で 5 分駆動すると約 30 ms のずれが出る。設計の欠陥ではなくエンジン側の下限。
- 2026-09-04: プロジェクトスキルを新規作成すると、セッション再起動なしで同一セッション中にハーネスが検出し呼び出せるようになる。なお `ListSkills` は claude.ai スキルの一覧なので、プロジェクトスキルの登録確認には使えない。
- 2026-09-05: WebGL の AudioClip はサンプリングレートがブラウザの AudioContext に合わせて変わる（22.05 kHz の WAV が frequency=44100 で返る）。frequency を前提にした計算は置かない。
- 2026-09-05: Chrome は OS のファイルダイアログが開いている間 requestAnimationFrame を止めるため、Unity WebGL のコルーチンも WaitForSecondsRealtime も進まない。ダイアログを閉じるまで Loaded が出ないのは正常で、実利用では選択と同時に閉じるので影響しない。
- 2026-09-05: StandaloneFileBrowser 1.3.4 は WebGL 用のラッパー実装を持たず、WebGL では `_platformWrapper` が null のまま NullReference になる。jslib は同梱されているが自前の DllImport が必要で、自前の jslib を書くのと手間が変わらない。
- 2026-09-05: Unity 6 WebGL では `DownloadHandlerAudioClip.GetContent` が返した直後の clip は `length=0` / `loadState=Unloaded` で、Unity 自身が LOG に「Trying to get length of sound which is not loaded yet」を出す。デコードはブラウザの decodeAudioData で非同期に進むため、`length` が正になるまで待つ（60 秒 WAV で 0.2 秒）。
- 2026-09-05: Burst は WebGL ビルド時に `Data/Plugins/lib_burst_generated.{cpp,wasm}` をプロジェクト直下へ吐く。Unity 標準の .gitignore には無いので `/Data/` を追加する。
- 2026-09-05: superpowers の executing-plans は main 直接作業を禁じるが、Unity プロジェクトで git worktree を作ると Library の再インポートが走る。ブランチをその場で切り替える方が現実的。

- 2026-09-05: Meshy が書き出す FBX は、テクスチャ 4 枚（2K BaseColor + 4K roughness/metallic）を Video ノードに Content として埋め込み、RelativeFilename が生成時の `/tmp/.../model.fbm/` を指す。メッシュ自体は 5k 頂点 / 10k 三角と軽く、ファイルサイズのほぼ全部がテクスチャ。
- 2026-09-05: Meshy FBX のメッシュは Lcl Rotation -90(X) と Lcl Scaling 100 が未適用のまま（UnitScaleFactor は 1.0 = m）。Blender で transform_apply + `apply_scale_options='FBX_SCALE_ALL'` で書き出すと UnitScaleFactor 100（cm）の素直な FBX になる。
- 2026-09-05: Mixamo の "Sorry, unable to map your existing skeleton." はボーンが 1 本も無い FBX でも出る。実体は既存骨のマッピング失敗ではなく FBX 解析の失敗で、埋め込みテクスチャとマテリアル参照を外したメッシュのみの FBX に差し替えたら通った。
- 2026-09-05: Mixamo はモデルの原点をグリッド原点に固定するため、ピボットが体の中心にある Meshy メッシュは身長の半分が床下に沈む。エクスポート前に頂点を移動して足元 Y=0・左右中心を原点に合わせる（オブジェクトの位置ではなく頂点座標を動かす）。
- 2026-09-05: 赤ちゃんウサギキャラの実寸は全高 0.8 m（Meshy 出力は 1.898 m だったので ModelImporter の globalScale=0.421515 で縮小）。耳が全高の約 15%（頭頂 1.62 / 全高 1.898）を占め、スケルトン高は Head ボーン Y=1.09 相当。1.8 m 人型前提のカメラ距離はこのキャラには当たらない。
- 2026-09-05: 「耳を除いた頭頂はどこか」は Z を 5 cm 刻みにビン分けして各スライスの頂点数を見ると決まる。頭部の塊は 1 スライス 90〜240 頂点、耳だけになると 15〜24 頂点に落ちるので境界が一意に読める。
- 2026-09-05: マテリアルを 1 つも持たない FBX は Unity 側でマテリアルを差し替えられない。remap の識別子（SourceAssetIdentifier の名前）が存在せず、SkinnedMeshRenderer には既定の `Lit` が入るだけ。Blender で名前付きの空マテリアルを 1 つ足して再エクスポートし、`ModelImporter.AddRemap` で外部 .mat に差し替える。
- 2026-09-05: Meshy の PBR マップは metallic が実質ゼロ（mean 0.38/255）、roughness だけが 0〜152 に分布する。URP Lit は R=metallic / A=smoothness の合成マップを要求するので、metallic と 255-roughness を合成した 1 枚を生成して `_MetallicGlossMap` に入れる。
- 2026-09-05: `Assets/Characters/` は「`AssetTools.CharacterFbx` 1 体だけが主役キャラ、他の FBX は全部ダンス」という前提。キャラを増やすと T ポーズクリップがダンスとして登録される。2026-09-05 に主役を BabyBunny.fbx に変更し、XBot.fbx は `Assets/Reference/` へ退避した。
- 2026-09-05: BabyBunny の顔は目・口・眉・チークが全部テクスチャに描かれており、目のジオメトリが存在しない。したがって瞬きはブレンドシェイプでは作れない。ブレンドシェイプでできるのは顔表面のスカッシュだけで、まぶたも眼球も無い。
- 2026-09-05: 顔の造作の実測（FBX 単位、全高 1.8979）。瞳 x[−0.159,+0.157] z[1.195,1.262]、眉 z[1.301,1.349]、口 x[−0.048,+0.047] z[1.090,1.178]。前髪のジオメトリが眉の帯（z 1.30〜1.35）に被さるため、眉を含むデカールは髪に貫通される。表情は目の形だけで担うことになる。
- 2026-09-05: blender-mcp のアドオンはバックグラウンドモード（`blender -b`）を明示的に拒否する（"commands would never execute"）。MCP 経由の操作は GUI の Blender が必要なので、再現性のあるパイプラインは `--background` の headless スクリプトに置き、MCP は対話的な確認だけに使う住み分けになる。`tools/unity.sh` と GUI Editor の関係と同じ。
- 2026-09-06: 表情の強制瞬きは切替後0.5拍を優先し、その後は通常瞬きへ戻る。通常瞬きが重なる場合の開眼遅延を許容する仕様であり、切替後0.5拍での開眼を保証しない。

## Open Questions
（未解決・要調査）
- 2026-09-04: 「5 分再生してドリフトしない」を自動回帰テストで守れるか。位相が絶対拍位置の純関数である限り、長時間テストは Mecanim 内部の累積を再測定するだけになり、このリポジトリのソース変更で壊せる対象が見つかっていない。
- 2026-09-05: ブラウザでの拍同期精度（`PlayScheduled` と `dspTime` の関係）は未計測。現状は目視・聴感の確認のみで、数値で測る手段が無い。

## Consolidated Principles
（統合パス専用。通常の更新処理から直接追記しない）
